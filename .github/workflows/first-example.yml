name: My First Workflow

on: [push,fork]

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My first Github Actions Job v4" 
            
            - name: Where am I
              run: pwd

            - name: Read file
              run: cat ./README.md